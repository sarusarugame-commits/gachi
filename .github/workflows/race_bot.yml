name: Boat Race Bot

on:
  schedule:
    # 1. 朝の部 (08:30〜)
    - cron: '30 23 * * *'
    # 2. 昼の部 (14:15〜)
    - cron: '15 5 * * *'
    # 3. 夜の部 (20:00〜)
    - cron: '0 11 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-race-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 360 # 最大6時間で強制終了（それまではループし続ける）

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy lightgbm joblib beautifulsoup4 lxml curl_cffi requests openai scikit-learn

      # ▼▼▼ ここを修正（無限ループ＆自動保存機能付き） ▼▼▼
      - name: Run Bot with Auto-Restart
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          PYTHONUNBUFFERED: '1'
        run: |
          # Gitの初期設定（ループの前に行う）
          git config --global user.name "github-actions[bot]"
          git config --global user.email "bot@noreply.github.com"

          echo "🔄 無限ループモードでBotを起動します..."

          # 無限ループ開始
          while true; do
            echo "------------------------------------------------"
            echo "🚀 Botを起動します"
            echo "------------------------------------------------"
            
            # Pythonを実行
            python main.py
            
            echo "⚠️ Botが停止しました。データを保存して再起動します..."
            
            # ▼▼▼ 停止のたびにデータを保存（ここに追加） ▼▼▼
            git fetch origin main
            git reset --soft origin/main
            git add race_data.db
            
            # 変更がある場合のみコミット＆プッシュ
            if git diff --staged --quiet; then
              echo "変更なし。スキップします。"
            else
              git commit -m "Auto-save DB after stop [skip ci]"
              git push origin main
              echo "💾 データを保存しました。"
            fi
            # ▲▲▲ 保存処理終了 ▲▲▲

            echo "💤 10秒待機後に再起動します..."
            sleep 10
          done
